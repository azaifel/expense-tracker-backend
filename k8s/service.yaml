apiVersion: v1
kind: Service
metadata:
  name: expense-tracker
  namespace: expense-tracker
spec:
  type: LoadBalancer
  selector:
    app: expense-tracker
  ports:
    - port: 80
      targetPort: 8080
